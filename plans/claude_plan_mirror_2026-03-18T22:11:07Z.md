# Plan: meow.sh

## Context
User wants a shell script that outputs "meow".

## Implementation

Create `meow.sh` in the repo root:

```bash
#!/usr/bin/env bash
echo "meow"
echo "meow"
```

Make it executable: `chmod +x meow.sh`

## Verification

Run `./meow.sh` — output should be `meow`.
